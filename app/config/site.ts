const SITE = {
  title: "TODO(boot): culiboot",
  /**
   * Used in meta tags. Sometimes shows up as the description in search results as well.
   */
  description: "TODO(boot): description",
};

export default SITE;
